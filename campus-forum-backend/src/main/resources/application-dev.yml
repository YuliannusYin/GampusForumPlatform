# ===== 开发环境配置 =====
spring:
  # 数据源：指向本地 MySQL
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/campus_forum?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8mb4&useSSL=false
    username: root
    password: root
  # Redis：指向本地 Redis
  data:
    redis:
      host: localhost
      port: 6379
      password:

# ===== 开发环境日志级别 =====
logging:
  level:
    root: info
    com.campus.forum: debug
